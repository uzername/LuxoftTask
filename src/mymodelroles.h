#ifndef MYMODELROLES_H
#define MYMODELROLES_H
#include <Qt>
/**
 * @brief The Roles enum is for roles in qml model
 */
enum Roles {
    RoleImagePath = Qt::DisplayRole,
    RolePositionX,
    RolePositionY,
};
#endif // MYMODELROLES_H
